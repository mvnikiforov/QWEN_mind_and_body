#  Сборка:  docker build -f deploy/Dockerfile -t probalance .
#  Запуск:  docker run -d -p 8080:80 probalance  → http://localhost:8080

FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine
COPY deploy/nginx-docker.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
